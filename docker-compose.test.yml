services:
  mongodb_test:
    container_name: mongodb_test
    image: mongo:7.0
    ports:
      - "27018:27017"
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')", "--quiet"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
